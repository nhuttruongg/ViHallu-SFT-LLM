# Core ML Framework
torch==2.7.1
torchvision==0.18.1
torchaudio==2.3.1

# Transformers & PEFT (đã test compatibility)
transformers==4.43.4
accelerate==0.30.1
peft==0.11.1
bitsandbytes==0.43.1

# Datasets & Tokenizers (chỉ dùng phiên bản khuyến nghị cho transformers 4.43+)
datasets==2.19.1
tokenizers==0.15.0   # Dùng đúng bản này, tránh conflict protobuf

# Scientific Computing
numpy==1.25.2        # Đã test ổn định với pandas 2+
pandas==2.0.3
scikit-learn==1.3.0

# Utilities (chỉ bản mới, không quá cao)
tqdm==4.66.1
sentencepiece==0.1.99
protobuf==4.23.4     # >=3.20.0 nhưng không dùng bản 5.x (transformers chưa support)
safetensors==0.4.0